name: JMXConnetionPool
version: 1.0
enabled: true
jmx:
  - object_name: app.monitor:type=MonitorExposure
    metrics:
      - attributes: MessageInCount, MessageOutCount, MessageFailedOutCount, MeanRateIn, MeanRateOut, LastIntervalInCount, LastIntervalOutCount
        type: simple
